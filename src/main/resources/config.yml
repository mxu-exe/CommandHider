version: 1

groups:
  default:
      inherit-groups: [vip]
      bypass-permission: commandhider.bypass.default
      commands:
        - msg
        - say
  vip:
      inherit-groups: [admin]
      bypass-permission: commandhider.bypass.vip
      commands:
        - ban
        - kick
  admin:
      inherit-groups: []
      bypass-permission: commandhider.bypass.admin
      commands:
        - plugins
        - version